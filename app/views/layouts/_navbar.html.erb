<!-- Fixed navbar -->
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'marqueeまとめ', root_path, class: 'navbar-brand' %>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav pull-right">
        <% if login? %>
          <li class="dropdown">
            <%= link_to '#', class: 'dropdown-toggle', data: { toggle: 'dropdown' } do %>
              <%= image_tag myimage, { width: 30, heidth: 30, style: 'margin:-10px 0px;' } %> <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu">
              <li><%= link_to 'My page', user_path(myname) %></li>
              <li><%= link_to 'Create site', new_site_path %></li>
              <li><%= link_to 'Sign out', logout_path %></li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to '/auth/twitter' do %>
              Sign in with Twitter
            <% end %>
          </li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>
