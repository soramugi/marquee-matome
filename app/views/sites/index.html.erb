<div id="sites" class="col-lg-12">
  <% @sites.each do |site| %>
    <div class="col-md-6">
      <%= link_to site_path(site.id) do %>
        <%= site.title %><br />
        <%= image_tag site.thumnail %>
      <% end %>
    </div>
  <% end %>
</div>
